<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:theme="@style/AppTheme"
        tools:context="byenkov.com.dices.diceroller.CharacterActivity"
        android:background="@color/colorPrimary"
        android:id="@+id/character_relative"
        android:tag="characters_fragment">
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/character_scroll_view">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/fragment_character_menu"
            android:orientation="vertical">

            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/add_character_button"
                android:layout_alignParentBottom="true"
                android:text="add new character"
                android:background="@color/colorPrimaryDark"
                android:textColor="@color/colorLight"
                />
        </LinearLayout>
    </ScrollView>

</RelativeLayout>